import React from 'react';
import ReactDOM from 'react-dom';
import './style.scss';

const title = 'My Minimal React Webpack Babel Setupt';

ReactDOM.render(
  <h1>{title}</h1>,
  document.getElementById('app'),
);
